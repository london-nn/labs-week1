class lab5
{
    static void Main(string[] args)
    {
        Console.WriteLine(NumberOfPagesLeft(450, 5));
    }

    static int NumberOfPagesLeft(int pageCount, int days)
    {
        int pagesPerDay = 40;
        int ReadenPages = 40 * days;
        int pagesLeft = pageCount - pages;

        return pagesLeft;
    }
}